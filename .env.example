# Renderの「Environment」から設定可（未設定でも動作）
PORT=3000

# 一時ファイルの保存先（未指定時は ./tmp ）
TMP_DIR=./tmp

# 1チャンクのJSON最大サイズ（GAS側は2〜5MB推奨）
MAX_JSON_SIZE=10mb

# CORSを有効化したい場合のみ 1
ENABLE_CORS=0

# 任意: クライアント認証（GAS側は x-api-key ヘッダで渡す）
API_KEY=
